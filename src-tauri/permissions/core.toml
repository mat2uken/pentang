"$schema" = "../gen/schemas/desktop-schema.json"

[[permission]]
identifier = "allow-core-api"
description = "Allow PoC getInfo/transform commands only (json + binary data plane)"
commands.allow = ["core_get_info", "core_transform"]
